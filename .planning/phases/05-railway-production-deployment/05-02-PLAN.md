---
phase: 05-railway-production-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - DEPLOY-02
  - DEPLOY-03
user_setup:
  - service: railway-collab-service
    why: "Hocuspocus collaboration server needs its own Railway service for WebSocket connections"
    dashboard_config:
      - task: "Create second service in same Railway project"
        location: "Railway Dashboard -> Project -> + New -> Service -> from same GitHub repo"

must_haves:
  truths:
    - "Hocuspocus collaboration server runs as a separate Railway service"
    - "Real-time collaboration (cursors, presence, sync) works for internal staff editing pages"
    - "File attachments uploaded in production persist after a redeploy"
  artifacts: []
  key_links:
    - from: "COLLAB_URL env var on main service"
      to: "collab Railway service public URL"
      via: "StaticModule injects into window.CONFIG"
      pattern: "COLLAB_URL=https://.*\\.up\\.railway\\.app"
---

<objective>
Create the collab Railway service and verify S3 file persistence end-to-end.

Purpose: Complete the two-service Railway architecture (DEPLOY-02) and verify file storage persistence (DEPLOY-03). The main service is already deployed from Plan 01 — this plan adds the collaboration server and runs the final smoke tests.

Output: Two Railway services running (main + collab), real-time collaboration functional, file uploads persisting across redeploys.
</objective>

<execution_context>
@/Users/rafaelandresberti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rafaelandresberti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-railway-production-deployment/05-CONTEXT.md
@.planning/phases/05-railway-production-deployment/05-RESEARCH.md
@.planning/phases/05-railway-production-deployment/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create collab Railway service and wire COLLAB_URL</name>
  <files>none — Railway dashboard operations only</files>
  <action>
Human must create a second Railway service. Claude cannot access Railway dashboard.

Step 1: Create collab service in Railway Dashboard -> Project -> "+ New" -> GitHub Repo -> select same docmost repo. Name it "collab".

Step 2: Configure collab service settings:
- Start Command: pnpm --filter server collab:prod
- Builder: DOCKERFILE (same Dockerfile as main)

Step 3: Set environment variables on the COLLAB service:
- NODE_ENV=production
- COLLAB_PORT=$PORT (Railway resolves $PORT to dynamically assigned port — do NOT hardcode 3001)
- DATABASE_URL=(same Railway PostgreSQL reference as main service)
- REDIS_URL=(same Railway Redis reference as main service)
- APP_SECRET=(same value as main service)

Step 4: Deploy collab service. Watch build logs — Docker build completes, collab server starts, deployment goes green.

Step 5: Get collab service public URL from Railway Dashboard -> collab service -> Settings -> Networking (e.g., https://collab-production-XXXX.up.railway.app).

Step 6: Set COLLAB_URL on the MAIN service in Railway Dashboard -> main service -> Variables:
- COLLAB_URL=https://(collab-service-url).up.railway.app
- This triggers a redeploy of the main service.

Step 7: Verify:
- curl -f https://client-production-dba5.up.railway.app/api/health/live (expect: ok — main still works after redeploy)
- Open app in browser, log in as admin/staff, open a page for editing
- Open same page in second browser tab or incognito
- Type in one tab — verify other tab shows changes in real-time
  </action>
  <verify>User reports collab service URL and real-time collaboration test result</verify>
  <acceptance_criteria>
    - Railway Dashboard shows two services: main (green) and collab (green)
    - COLLAB_URL is set on the main service
    - Real-time editing in one browser tab is visible in another tab on the same page
    - curl https://client-production-dba5.up.railway.app/api/health/live still returns "ok"
  </acceptance_criteria>
  <done>Collab Railway service deployed, COLLAB_URL wired, real-time collaboration working in production</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify S3 file persistence across redeploy</name>
  <files>none — verification only</files>
  <action>
Human verifies that file uploads persist across Railway redeploys.

Step 1: Open https://client-production-dba5.up.railway.app in browser. Log in as admin. Open or create a page. Upload an image attachment (drag-and-drop or paste). Verify image displays. Note the image URL (right-click -> copy image address).

Step 2: Trigger a redeploy in Railway Dashboard -> main service -> click "Redeploy". Wait for deployment to go green.

Step 3: Refresh the page with the uploaded image. Verify image still displays correctly.

If image is broken after redeploy:
- Check STORAGE_DRIVER is "s3" (not "local")
- Check all AWS_S3_* env vars are set
- Check AWS_S3_URL is NOT set (must be unset)
- Check R2 bucket exists and API token has read/write permissions
  </action>
  <verify>User confirms image persists after redeploy, or reports issue</verify>
  <acceptance_criteria>
    - Image uploaded before redeploy is still visible and loads correctly after redeploy
    - Image URL returns HTTP 200 after redeploy
  </acceptance_criteria>
  <done>File attachments persist across Railway redeploys via S3/R2 storage — DEPLOY-03 fully verified</done>
</task>

</tasks>

<verification>
After both tasks complete:
- Railway Dashboard shows two services: main (green) and collab (green) — DEPLOY-02 verified
- Real-time collaboration works between two browser tabs — DEPLOY-02 fully verified
- Uploaded file persists after redeploy — DEPLOY-03 verified
- All DEPLOY-01 through DEPLOY-06 requirements are satisfied across Plan 01 and Plan 02
</verification>

<success_criteria>
- Two Railway services (main + collab) are both deployed and green
- COLLAB_URL is set on the main service pointing to the collab service URL
- Real-time collaboration (cursors/presence/sync) works in production
- File upload persists across a Railway redeploy (S3/R2 storage confirmed working)
- All six DEPLOY requirements are verified
</success_criteria>

<output>
After completion, create `.planning/phases/05-railway-production-deployment/05-02-SUMMARY.md`
</output>
