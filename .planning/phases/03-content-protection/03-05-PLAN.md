---
phase: 03-content-protection
plan: 05
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/client/src/components/ContentProtection.tsx
  - apps/client/src/pages/settings/content-security.tsx
  - apps/client/src/components/ScreenshotDetection.tsx
autonomous: true
gap_closure: true
requirements:
  - PROT-01
  - PROT-02
  - PROT-03
  - PROT-04
  - PROT-05
  - PROT-V2-02

must_haves:
  truths:
    - "ContentProtection.tsx calls all hooks (useRef, useAtom, useCallback, useEffect) before any conditional return"
    - "content-security.tsx calls all hooks (useState, useEffect) before any conditional return"
    - "ScreenshotDetection.tsx reads attemptCount from response.data.status, not response.data"
  artifacts:
    - path: "apps/client/src/components/ContentProtection.tsx"
      provides: "Hooks-compliant content protection component"
      contains: "if (!isProtected)"
    - path: "apps/client/src/pages/settings/content-security.tsx"
      provides: "Hooks-compliant admin security page"
      contains: "useEffect"
    - path: "apps/client/src/components/ScreenshotDetection.tsx"
      provides: "Correct API response field access"
      contains: "response.data.status"
  key_links:
    - from: "apps/client/src/components/ScreenshotDetection.tsx"
      to: "GET /api/security/screenshot-status"
      via: "response.data.status.attemptCount"
      pattern: "response\\.data\\.status"
---

<objective>
Fix two verification gaps from 03-VERIFICATION.md: React Rules of Hooks violations in ContentProtection.tsx and content-security.tsx, and API response field mismatch in ScreenshotDetection.tsx.

Purpose: Eliminate runtime crash risk from conditional hook calls and fix broken suspension status detection on page load.
Output: Three corrected files with all hooks called unconditionally and correct API response destructuring.
</objective>

<execution_context>
@/Users/rafaelandresberti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rafaelandresberti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-protection/03-VERIFICATION.md

<interfaces>
<!-- Backend response shape from screenshot-detection.controller.ts lines 40-51 -->
From apps/server/src/integrations/security/screenshot-detection.controller.ts:
```typescript
// getScreenshotStatus returns:
{
  success: true,
  status: {
    attemptCount: number,
    suspended: boolean
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix React Rules of Hooks violations in ContentProtection.tsx and content-security.tsx</name>
  <files>apps/client/src/components/ContentProtection.tsx, apps/client/src/pages/settings/content-security.tsx</files>
  <read_first>
    - apps/client/src/components/ContentProtection.tsx (full file — understand current hook placement relative to early return at line 37)
    - apps/client/src/pages/settings/content-security.tsx (full file — understand current hook placement relative to early return at line 27-29)
  </read_first>
  <action>
**ContentProtection.tsx** — Move all hooks before the `if (!isProtected)` early return at line 37.

Current structure (BROKEN):
```
useRef (line 29) -- before return, OK
useAtom (line 32) -- before return, OK
if (!isProtected) return -- LINE 37
useCallback x8 (lines 42-170) -- AFTER return, VIOLATION
useEffect x2 (lines 172-283) -- AFTER return, VIOLATION
```

Target structure (FIXED):
```
useRef (line 29) -- keep
useAtom (line 32) -- keep
useCallback x8 -- MOVE before early return
useEffect x2 -- MOVE before early return, add `if (!isProtected) return;` as first line of each effect body
if (!isProtected) return <>{children}</> -- keep, now AFTER all hooks
return (...) -- protected JSX
```

Specific changes:
1. Move all 8 `useCallback` declarations (handleKeyDown, handleContextMenu, handleDragStart, handleSelectStart, handleCopy, handleCut, handleTouchStart, handleTouchEnd) to immediately after `const watermarkUri = ...` (line 34), BEFORE the `if (!isProtected)` check.
2. Move both `useEffect` blocks to after the useCallback declarations but BEFORE the `if (!isProtected)` check. Add `if (!isProtected) return;` as the very first line inside each useEffect callback body so they no-op when protection is off (the first useEffect already has `if (!element) return;` which covers this since protectionRef won't be set, but add the explicit guard for clarity; the second useEffect for beforeprint needs the guard added).
3. Keep the `if (!isProtected) { return <>{children}</>; }` line — just move it to AFTER all hook declarations.
4. Keep the return JSX block unchanged at the bottom.

**content-security.tsx** — Move useEffect before the `if (!isAdmin)` early return at line 27.

Current structure (BROKEN):
```
useUserRole (line 23) -- before return, OK
useState x2 (lines 24-25) -- before return, OK
if (!isAdmin) return null -- LINE 27-29
fetchViolations function -- AFTER return
useEffect (line 41) -- AFTER return, VIOLATION
```

Target structure (FIXED):
```
useUserRole (line 23) -- keep
useState x2 (lines 24-25) -- keep
useEffect with fetchViolations inlined -- MOVE before early return, add `if (!isAdmin) return;` as first line of effect body
if (!isAdmin) return null -- keep, now AFTER all hooks
handleReinstate, formatDate -- keep after early return (these are plain functions, not hooks)
return (...) -- JSX
```

Specific changes:
1. Move the `useEffect` to immediately after the `useState` declarations (after line 25).
2. Inline the `fetchViolations` async function INSIDE the useEffect callback (or define it there as a local async function and call it).
3. Add `if (!isAdmin) return;` as the first line inside the useEffect body so it no-ops for non-admins.
4. Add `isAdmin` to the useEffect dependency array: `useEffect(() => { ... }, [isAdmin])`.
5. Keep the `if (!isAdmin) return null;` early return AFTER the useEffect.
6. The `handleReinstate` and `formatDate` functions are NOT hooks — they can stay after the early return.
  </action>
  <verify>
    <automated>cd /Users/rafaelandresberti/docmost && npx vitest run apps/client/src/components/ContentProtection.test.tsx apps/client/src/pages/settings/content-security.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "useCallback\|useEffect\|useRef\|useAtom\|useState" apps/client/src/components/ContentProtection.tsx shows ALL hook calls appear at line numbers LOWER than the line containing "if (!isProtected)"
    - grep -n "useEffect\|useState\|useUserRole" apps/client/src/pages/settings/content-security.tsx shows ALL hook calls appear at line numbers LOWER than the line containing "if (!isAdmin)"
    - No `useCallback` or `useEffect` appears after any `return` statement that is NOT inside a function body or JSX
  </acceptance_criteria>
  <done>All React hooks in ContentProtection.tsx and content-security.tsx are called unconditionally before any early return. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix API response field access in ScreenshotDetection.tsx</name>
  <files>apps/client/src/components/ScreenshotDetection.tsx</files>
  <read_first>
    - apps/client/src/components/ScreenshotDetection.tsx (lines 225-260 — the checkSuspensionStatus useEffect)
    - apps/server/src/integrations/security/screenshot-detection.controller.ts (lines 40-51 — getScreenshotStatus return shape)
  </read_first>
  <action>
In `ScreenshotDetection.tsx`, fix line 237 where `const { attemptCount } = response.data` reads the wrong level of the API response.

The backend `getScreenshotStatus` controller method (screenshot-detection.controller.ts lines 43-50) returns:
```json
{ "success": true, "status": { "attemptCount": 3, "suspended": true } }
```

The axios response puts this under `response.data`, so the full path is `response.data.status.attemptCount`.

Change line 237 from:
```typescript
const { attemptCount } = response.data;
```

To:
```typescript
const attemptCount = response.data?.status?.attemptCount ?? response.data?.attemptCount ?? 0;
```

This reads from the correct nested path (`response.data.status.attemptCount`) with a fallback to the root level for defensive compatibility. The `?? 0` at the end ensures attemptCount is always a number.

Also update line 240 — since attemptCount now defaults to 0, simplify:
```typescript
setAttemptCount(attemptCount);
```
(Remove the `|| 0` since the fallback chain already provides 0.)
  </action>
  <verify>
    <automated>cd /Users/rafaelandresberti/docmost && npx vitest run apps/client/src/components/ScreenshotDetection.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "response.data.status" apps/client/src/components/ScreenshotDetection.tsx returns a match (confirms reading from nested .status path)
    - grep "response.data.attemptCount" apps/client/src/components/ScreenshotDetection.tsx does NOT return a match as a standalone destructure (the old broken pattern is gone)
  </acceptance_criteria>
  <done>ScreenshotDetection.tsx reads attemptCount from `response.data.status.attemptCount` matching the backend response shape `{ success, status: { attemptCount, suspended } }`. Suspended users now correctly see the account-locked modal on page load.</done>
</task>

</tasks>

<verification>
1. All existing phase 03 tests pass: `cd /Users/rafaelandresberti/docmost && npx vitest run apps/client/src/components/ContentProtection.test.tsx apps/client/src/components/ScreenshotDetection.test.tsx apps/client/src/pages/settings/content-security.test.tsx --reporter=verbose`
2. No Rules of Hooks violations remain: grep confirms all hook calls in ContentProtection.tsx appear before the `if (!isProtected)` line, and all hook calls in content-security.tsx appear before the `if (!isAdmin)` line.
3. API response field access is correct: grep confirms `response.data.status` is used in ScreenshotDetection.tsx.
</verification>

<success_criteria>
- ContentProtection.tsx has zero hooks after the `if (!isProtected)` early return
- content-security.tsx has zero hooks after the `if (!isAdmin)` early return
- ScreenshotDetection.tsx reads attemptCount from `response.data.status` not `response.data`
- All existing vitest tests for these three files pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-protection/03-05-SUMMARY.md`
</output>
